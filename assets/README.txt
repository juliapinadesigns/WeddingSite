Drop your hero photo here as: hero.jpg

Then open css/style.css and in the .hero__media rule, uncomment the
background-image line (it points to ../assets/hero.jpg). The gradient
will stay as a fallback until the photo loads.
